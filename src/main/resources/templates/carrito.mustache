<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Shopping bag</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
          background-color: #f8f9fa;
        }
        .cart-item {
          border-bottom: 1px solid #ddd;
          padding: 15px 0;
        }
        .cart-item img {
          width: 100px;
          height: auto;
          object-fit: cover;
        }
        .cart-summary {
          background-color: #fff;
          border: 1px solid #ddd;
          padding: 20px;
          border-radius: 8px;
        }
        .btn-remove {
          color: #dc3545;
          border: none;
          background: none;
        }
    </style>
</head>
<body>
<div class="container mt-5">
    <h2 class="mb-4">🛒 Your Shopping bag</h2>

    {{#items}}
        <div class="row cart-item align-items-center">
            <div class="col-md-2">
                <img src="{{foto}}" alt="{{nombre}}">
            </div>
            <div class="col-md-4">
                <h5>{{nombre}}</h5>
                <p>{{descripcion}}</p>
            </div>
            <div class="col-md-2 text-center">
                <p class="mb-0">Unit: ${{precio}}</p>
                <p class="text-muted">Qty: {{cantidad}}</p>
            </div>
            <div class="col-md-2 text-center">
                <strong>Total: ${{total}}</strong>
            </div>
            <div class="col-md-2 text-end">
                <form method="POST" action="/carrito/remove">
                    <input type="hidden" name="id" value="{{id}}">
                    <button type="submit" class="btn-remove">🗑 Remove</button>
                </form>
            </div>
        </div>
    {{/items}}

    {{^items}}
        <p class="text-muted">Your cart is empty</p>
    {{/items}}

    <div class="row mt-4">
        <div class="col-md-6 offset-md-6">
            <div class="cart-summary">
                <h5>Total a pagar: <strong>${{totalGeneral}}</strong></h5>
                <form method="POST" action="/carrito/checkout">
                    <button type="submit" class="btn btn-dark w-100 mt-3">Checkout</button>
                </form>
            </div>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
