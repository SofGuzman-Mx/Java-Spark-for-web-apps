<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Shopping bag</title>
    <!-- CORRECCIÓN EN LA URL DEL CSS DE BOOTSTRAP -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
          background-color: #f8f9fa;
        }
        .cart-item {
          border-bottom: 1px solid #ddd;
          padding: 15px 0;
        }
        .cart-item img {
          width: 100px;
          height: 100px; /* Alto fijo para consistencia */
          object-fit: cover;
        }
        .cart-summary {
          background-color: #fff;
          border: 1px solid #ddd;
          padding: 20px;
          border-radius: 8px;
        }
        .btn-remove {
          color: #dc3545;
          border: none;
          background: none;
          padding: 0;
          font-size: 1.2rem;
        }
    </style>
</head>
<body>

<!-- BARRA DE NAVEGACIÓN AÑADIDA DE VUELTA -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">Menu</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" href="/catalog">Items</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/offers">Sales</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="/carrito">Shopping cart</a> <!-- Marcado como activo -->
                </li>
            </ul>
        </div>
    </div>
</nav>

<div class="container mt-5">
    <h2 class="mb-4">🛒 Your Shopping bag</h2>

    {{#items}}
        <div class="row cart-item align-items-center">
            <div class="col-md-2">
                <img src="{{foto}}" alt="{{nombre}}" class="img-fluid rounded">
            </div>
            <div class="col-md-4">
                <h5>{{nombre}}</h5>
                <p class="text-muted small d-none d-md-block">{{descripcion}}</p> <!-- Oculta descripción en pantallas pequeñas -->
            </div>
            <div class="col-md-2 text-center">
                <p class="mb-0">Unit: ${{precio}}</p>
                <p class="text-muted">Qty: {{cantidad}}</p>
            </div>
            <div class="col-md-2 text-center">
                <strong>Subtotal: ${{total}}</strong>
            </div>
            <div class="col-md-2 text-end">
                <!-- Por ahora, el botón de eliminar no es funcional, pero lo mostramos -->
                <button type="button" class="btn-remove" title="Remove item" onclick="alert('Funcionalidad para eliminar no implementada aún.');">🗑</button>
            </div>
        </div>
    {{/items}}

    {{^items}}
        <div class="alert alert-info mt-4" role="alert">
            Your cart is empty. <a href="/catalog" class="alert-link">Continue shopping!</a>
        </div>
    {{/items}}

    <!-- SOLO MUESTRA EL RESUMEN DE PAGO SI HAY ITEMS EN EL CARRITO -->
    {{#items.size}}
        <div class="row mt-4">
            <div class="col-md-6 offset-md-6">
                <div class="cart-summary">
                    <h5 class="text-end">Total a pagar: <strong class="ms-3">${{totalGeneral}}</strong></h5>
                    <!-- FORMULARIO DE CHECKOUT ÚNICO Y FUNCIONAL -->
                    <form method="POST" action="/checkout">
                        <button type="submit" class="btn btn-dark w-100 mt-3">Proceed to Checkout</button>
                    </form>
                </div>
            </div>
        </div>
    {{/items.size}}

</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>