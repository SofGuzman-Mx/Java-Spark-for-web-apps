<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - E-commerce</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen">

<div class="bg-white p-8 rounded-lg shadow-md w-full max-w-md">
    <h2 class="text-2xl font-bold mb-6 text-center text-gray-800">Login</h2>

    <!-- Le añadimos un ID al formulario para identificarlo con JS -->
    <form id="loginForm" method="POST" action="/api/login">
        <!-- Tus campos de input se quedan igual -->
        <div class="mb-4">
            <label for="nombre" class="block text-gray-700 font-bold mb-2">Username</label>
            <input type="text" id="nombre" name="nombre" class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="Your username" required>
        </div>
        <div class="mb-6">
            <label for="password" class="block text-gray-700 font-bold mb-2">Password</label>
            <input type="password" id="password" name="password" class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="********" required>
        </div>
        <button type="submit" class="w-full bg-blue-600 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-lg focus:outline-none focus:shadow-outline transition duration-200">
            Login
        </button>
    </form>
    <p class="mt-4 text-center text-gray-600">
        Don't have an account yet? <a href="/signup" class="text-blue-600 hover:underline">Sign Up</a>
    </p>
</div>

<!-- ===== SECCIÓN DE JAVASCRIPT AÑADIDA ===== -->
<script>
    // 1. Obtenemos el formulario por su ID
    const loginForm = document.getElementById('loginForm');

    // 2. Añadimos un "escuchador" para el evento de envío (submit)
    loginForm.addEventListener('submit', function(event) {

        // 3. ¡MUY IMPORTANTE! Prevenimos el comportamiento por defecto del formulario (que recarga la página)
        event.preventDefault();

        // 4. Creamos un objeto FormData para recolectar los datos del formulario
        const formData = new FormData(loginForm);

        // 5. Usamos la API Fetch para enviar los datos al backend
        fetch('/api/login', {
            method: 'POST',
            // El body debe estar en formato 'x-www-form-urlencoded' porque tu backend usa req.queryParams()
            body: new URLSearchParams(formData)
        })
        .then(response => {
            // Verificamos si la respuesta del servidor fue exitosa (status 200-299)
            if (response.ok) {
                return response.json(); // Si es exitosa, procesamos el JSON
            }
            // Si hubo un error (ej. 401, 500), también procesamos el JSON para obtener el mensaje de error
            return response.json().then(errorData => {
                // Creamos un error personalizado para pasarlo al bloque .catch()
                throw new Error(errorData.message || 'An unknown error occurred.');
            });
        })
        .then(data => {
            // Este bloque se ejecuta SI LA RESPUESTA FUE EXITOSA
            if (data.success && data.redirectUrl) {
                // Redirigimos al usuario a la página de catálogo
                window.location.href = data.redirectUrl;
            }
        })
        .catch(error => {
            // Este bloque se ejecuta SI HUBO UN ERROR (de red o del servidor)
            console.error('Login Error:', error);
            // ¡AQUÍ ESTÁ LA MAGIA! Mostramos la alerta con el mensaje de error del backend.
            alert(error.message);
        });
    });
</script>

</body>
</html>