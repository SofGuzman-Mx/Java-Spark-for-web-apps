<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Special Prices</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
          font-family: 'Helvetica Neue', sans-serif;
          background-color: #fff;
          color: #000;
        }
        .navbar-zara {
          background-color: #fff;
          border-bottom: 1px solid #ddd;
        }
        .navbar-zara .nav-link {
          color: #000;
          font-weight: 500;
          margin: 0 15px;
        }
        .product-card {
          border: none;
          text-align: center;
        }
        .product-img {
          height: 300px;
          object-fit: cover;
          margin-bottom: 15px;
        }
        .price-original {
          text-decoration: line-through;
          color: #888;
          margin-right: 10px;
        }
        .price-final {
          font-weight: bold;
          color: #000;
        }
    </style>
</head>
<body>

<!-- Barra de navegación estilo Zara -->
<nav class="navbar navbar-expand-lg navbar-zara">
    <div class="container justify-content-center">
        <ul class="navbar-nav">
            <li class="nav-item"><a class="nav-link" href="/catalog">VIEW ALL</a></li>
            <li class="nav-item"><a class="nav-link" href="/offers">SPECIAL PRICES</a></li>
            <li class="nav-item"><a class="nav-link" href="/carrito">SHOPPING BAG</a></li>
        </ul>
    </div>
</nav>

<!-- Sección de productos en oferta -->
<div class="container mt-5">
    <h2 class="text-center mb-4">Special Prices</h2>
    <div class="row">
        {{#items}}
            <div class="col-md-4 mb-5">
                <div class="card product-card">
                    <img src="{{foto}}" class="product-img" alt="{{nombre}}">
                    <div class="card-body">
                        <h5 class="card-title">{{nombre}}</h5>
                        <p class="card-text">{{descripcion}}</p>
                        <p class="card-text">
                            <span class="price-original">${{precioOriginal}}</span>
                            <span class="price-final">${{prec}}</span>
                        </p>
                        <form method="POST" action="/carrito/add">
                            <input type="hidden" name="id" value="{{id}}">
                            <button type="submit" class="btn btn-outline-dark w-100">Shopping bag</button>
                        </form>
                    </div>
                </div>
            </div>
        {{/items}}
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
